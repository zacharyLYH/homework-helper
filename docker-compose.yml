services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    environment:
      - DATABASE_PATH=/app/data/homework_helper.db #volumes is at app/data/... on docker. local dev is just at /data/...
      - DEBUG_DATABASE_PATH=/app/data/debug.db
    volumes:
      - ./data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  frontend-debug:
    build: ./frontend-debug
    ports:
      - "81:80"
    depends_on:
      - backend
